services:
  - type: web
    name: orion-ia
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: HF_API_KEY
        sync: false
